import { createRouter } from "vue-router";
import { createWebHistory } from "vue-router";

// import components/pages later


const routes [{
    path: ,
    
},

]




const router = createRouter({
    history: createWebHistory(), routes
})

export default router;